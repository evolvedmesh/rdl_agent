node_modules/
dist/
.env
.env.*
*.local.json
confidential.json
secrets.json

# Electrobun/Hutch generated state: the devkit SDK projection Hutch materialises
# per-project, and the app bundles it builds. All reproducible from hutch.config.ts.
.hutch/
.cottontail-tmp/
build/
artifacts/

# spike working state — renders, PDFs, images, measurement output
.spike/
out/
*.pdf
*.png

# …but the test fixtures are checked in, so the suite runs anywhere (incl. CI)
!test/fixtures/**
